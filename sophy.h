/* typedef struct {
	const char** com;
	const int i;
	const Window w;
}

struct key {
	unsigned int mod;
	KeySym keysym;
	void (*function)(const Arg arg);
	const Arg arg;
}

typredef struct client {
	Window w, frame;
	struct client *next, *prev;
	int f, wx, wy;
	unsigned int ww, wh;
	unsigned char fs;
} client;

void run(const Arg arg); */
